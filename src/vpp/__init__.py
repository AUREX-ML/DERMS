"""VPP — Virtual Power Plant orchestration layer.

Three sub-packages:
- ``edge``: Site-edge agents (MQTT bridge, dispatch, offline buffer, Modbus).
- ``cloud``: ThingsBoard cloud integration (REST client, asset provisioner).
- ``aggregator``: VPP-level portfolio aggregation and dispatch.
"""
